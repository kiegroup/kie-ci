---
- name: Download GH-CLI repo config
  get_url:
    url: https://cli.github.com/packages/rpm/gh-cli.repo
    dest: /etc/yum.repos.d/gh-cli.repo
    owner: root
    group: root
    mode: 0644

- name: Install packages
  yum:
    pkg: ['gh']
    state: latest

...
